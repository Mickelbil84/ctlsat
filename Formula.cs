using System;
namespace CTLSAT
{
    public enum LogicOperator
    {
        EXISTS, ALL,
        AND, OR, NOT, IMP,
        EF, AF, EG, AG,
        EX, AX, EU, AU, ER, AR,
    }

    public class Formula
    {
        public Formula()
        {
            
        }
    }
}
